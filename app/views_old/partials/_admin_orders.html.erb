<tr class='order' data-status="<%= order.status %>" id="<%= order.id.to_s %>">
	  <td><%= order.user.username %></td>
	  <td><%= order.status.capitalize %></td>
	  <td><%= order.created_at %></td>
	  <td><%= number_to_currency(order.total_price) %></td>

	  <td><%= link_to "Order ##{"%07d" % order.id.to_s}", "#" %></td>

		<td>
			<%= link_to "Cancel", "#", method: :patch if order.status == "paid" || order.status == "ordered" %>
			<%= link_to "Mark as paid", "#", method: :patch if order.status == "ordered" %>
			<%= link_to "Mark as completed", "#", method: :patch if order.status == "paid" %>
		</td>
</tr>
