<%= render partial: 'partials/show_auction', locals: { auction: @auction, business: @business, time_left: @time_left } %>

<%= link_to "End Auction", declare_winner_path([@auction, @business]), method: :post, class: "hidden", id: 'win'%>
<div class="center">
  <%=link_to "Back to Humans", auctions_path %>
</div>
<ul>
  <div class="container">
    <%= render partial: 'partials/review_card', collection: @reviews, as: :review %>
  </div>
</ul>
